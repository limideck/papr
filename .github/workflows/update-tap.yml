name: Update Homebrew tap

# When a GitHub Release is published, bump the cask in the l0ng-ai/homebrew-papr
# tap to the new version + per-arch sha256 so `brew upgrade --cask papr` picks it
# up. Runs on `published` (not the draft that release.yml first creates) because
# a draft release's asset URLs 404 for anonymous downloads — we need the public
# assets to hash them. `workflow_dispatch` allows re-running against any tag.

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to sync to the tap (e.g. v0.7.0)"
        required: true

jobs:
  update-tap:
    runs-on: ubuntu-latest
    # Never bump the stable tap from a pre-release. A manual workflow_dispatch
    # has no release payload (prerelease is null), so it still runs.
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.release.prerelease == false }}
    steps:
      - name: Resolve tag / version
        id: v
        run: |
          TAG="${{ github.event.release.tag_name || github.event.inputs.tag }}"
          if [ -z "$TAG" ]; then echo "no tag resolved" >&2; exit 1; fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"

      - name: Download dmgs and hash them
        id: sha
        run: |
          V="${{ steps.v.outputs.version }}"
          BASE="https://github.com/${{ github.repository }}/releases/download/${{ steps.v.outputs.tag }}"
          curl -fSL --retry 3 -o arm.dmg "$BASE/Papr_${V}_aarch64.dmg"
          curl -fSL --retry 3 -o x64.dmg "$BASE/Papr_${V}_x64.dmg"
          echo "arm=$(sha256sum arm.dmg | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"
          echo "x64=$(sha256sum x64.dmg | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"

      - name: Download CLI tarballs and hash them
        id: cli
        run: |
          BASE="https://github.com/${{ github.repository }}/releases/download/${{ steps.v.outputs.tag }}"
          curl -fSL --retry 3 -o cli-arm-mac.tgz "$BASE/papr-aarch64-apple-darwin.tar.gz"
          curl -fSL --retry 3 -o cli-x64-mac.tgz "$BASE/papr-x86_64-apple-darwin.tar.gz"
          curl -fSL --retry 3 -o cli-linux.tgz   "$BASE/papr-x86_64-unknown-linux-gnu.tar.gz"
          echo "arm_mac=$(sha256sum cli-arm-mac.tgz | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"
          echo "x64_mac=$(sha256sum cli-x64-mac.tgz | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"
          echo "linux=$(sha256sum cli-linux.tgz | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"

      - name: Check out tap
        uses: actions/checkout@v4
        with:
          repository: l0ng-ai/homebrew-papr
          ssh-key: ${{ secrets.TAP_DEPLOY_KEY }}
          path: tap

      - name: Patch the cask
        run: |
          V="${{ steps.v.outputs.version }}"
          ARM="${{ steps.sha.outputs.arm }}"
          X64="${{ steps.sha.outputs.x64 }}"
          f=tap/Casks/papr.rb
          # Replace only the quoted contents, preserving each line's alignment.
          sed -i -E "s|(version \")[^\"]*|\1${V}|" "$f"
          sed -i -E "s|(sha256 arm: +\")[^\"]*|\1${ARM}|" "$f"
          sed -i -E "s|(intel: +\")[^\"]*|\1${X64}|" "$f"
          echo "----- patched cask -----"
          cat "$f"
          # Fail loudly if any field didn't land, rather than pushing a stale cask.
          grep -q "version \"${V}\"" "$f"
          grep -q "\"${ARM}\"" "$f"
          grep -q "\"${X64}\"" "$f"

      - name: Generate the CLI formula
        run: |
          V="${{ steps.v.outputs.version }}"
          BASE="https://github.com/${{ github.repository }}/releases/download/v#{version}"
          mkdir -p tap/Formula
          # Regenerated wholesale each release — no placeholder file needs to
          # pre-exist in the tap, and there's no sed to drift out of alignment.
          cat > tap/Formula/papr-cli.rb <<EOF
          class PaprCli < Formula
            desc "Agent-facing CLI over your Papr RSS feeds"
            homepage "https://github.com/${{ github.repository }}"
            version "${V}"
            license "MIT"

            on_macos do
              on_arm do
                url "${BASE}/papr-aarch64-apple-darwin.tar.gz"
                sha256 "${{ steps.cli.outputs.arm_mac }}"
              end
              on_intel do
                url "${BASE}/papr-x86_64-apple-darwin.tar.gz"
                sha256 "${{ steps.cli.outputs.x64_mac }}"
              end
            end

            on_linux do
              on_intel do
                url "${BASE}/papr-x86_64-unknown-linux-gnu.tar.gz"
                sha256 "${{ steps.cli.outputs.linux }}"
              end
            end

            def install
              bin.install "papr"
            end

            test do
              assert_match "papr", shell_output("#{bin}/papr --help")
            end
          end
          EOF
          echo "----- generated formula -----"
          cat tap/Formula/papr-cli.rb

      - name: Commit and push
        run: |
          cd tap
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Casks/papr.rb Formula/papr-cli.rb
          if git diff --cached --quiet; then
            echo "cask + formula already up to date — nothing to push"
            exit 0
          fi
          git commit -m "papr ${{ steps.v.outputs.version }}"
          git push
